<UserControl x:Class="Jarvis.ProjectJarvis.Views.UserControls.Information.UpdatePointUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Content>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="•" Foreground="{DynamicResource Color.Gray}" FontFamily="{DynamicResource Font.SegoeUIVF}"
                       FontWeight="Medium" FontSize="13" />
            <TextBlock Margin="8,0,0,0" Text="{Binding Title}" Foreground="{DynamicResource Color.Gray}"
                       FontFamily="{DynamicResource Font.SegoeUIVF}" FontWeight="Medium" FontSize="13" />
        </StackPanel>
    </UserControl.Content>
</UserControl>