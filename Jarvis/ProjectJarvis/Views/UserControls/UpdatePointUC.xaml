<UserControl x:Class="Jarvis.ProjectJarvis.Views.UserControls.UpdatePointUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Content>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="•" Foreground="#696969" FontFamily="{DynamicResource Font.SegoeUIVF}" FontWeight="Medium" FontSize="13"/>
            <TextBlock Margin="8,0,0,0" Text="{Binding Title}" Foreground="#696969" FontFamily="{DynamicResource Font.SegoeUIVF}" FontWeight="Medium" FontSize="13"/>
        </StackPanel>
    </UserControl.Content>
</UserControl>
